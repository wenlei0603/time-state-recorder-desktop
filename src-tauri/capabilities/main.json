{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal capability for the primary Time State Recorder desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
